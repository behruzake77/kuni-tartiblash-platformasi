/**
 * Clerk integration stub
 * ----------------------------------------------------------------------------
 * 1. npm i @clerk/nextjs
 * 2. NEXT_PUBLIC_ORDO_AUTH=clerk
 * 3. NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY / CLERK_SECRET_KEY
 * 4. Replace resolveAuthProvider() clerk branch with real SDK calls
 * 5. Wrap root layout with <ClerkProvider>
 *
 * See DEPLOY.md § Auth modes
 */
export const CLERK_SETUP = true;
